---
status: in_progress
type: quick
created: 2026-05-01
---

# Quick Task 20260501-153648: reFee Review Fixes

## Goal

Fix the validated reFee sweep review findings:

- TRC20 sweep receipt must be checked before treating a sweep as successful.
- reFee acquire should reduce duplicate paid rental risk after transient polling or fullnode check failures.
- `REFEE.api_base_url` must not allow unsafe non-HTTPS API key destinations.

## Tasks

### 1. Add failing regression tests

Files:
- `tests/test_refee_energy_accounting.py`
- `tests/test_phase2_review_fixes.py`

Action:
- Add RED tests for failed TRC20 receipt handling, retry-before-new-order energy recheck, transient poll retry behavior, and non-HTTPS `api_base_url` rejection.

Verify:
- Each new test fails for the expected missing behavior before production changes.

Done:
- RED failures observed and recorded.

### 2. Implement minimal fixes

Files:
- `app/tasks.py`
- `app/energy_provider.py`
- `app/refee.py`

Action:
- Validate TRC20 receipt result before success logging/release/return.
- Recheck receiver energy before creating a reFee order and tolerate transient poll failures until deadline.
- Validate `api_base_url` as HTTPS, allowing localhost HTTP for tests/local development only.

Verify:
- Focused tests pass.

Done:
- Behavior fixed without unrelated refactors.

### 3. Final verification and artifacts

Files:
- `.planning/quick/20260501-153648-refee-review-fixes/20260501-153648-SUMMARY.md`
- `.planning/STATE.md`

Action:
- Run full test discovery in the Python 3.12 test venv.
- Record summary and update project state.

Verify:
- `/tmp/tron-shkeeper-py312-venv/bin/python -m unittest discover -s tests` passes.

Done:
- Code and GSD artifact commits created.
